import { TournamentYearPipe } from './tournament-year.pipe';

describe('TournamentYearPipe', () => {
  it('create an instance', () => {
    const pipe = new TournamentYearPipe();
    expect(pipe).toBeTruthy();
  });
});
